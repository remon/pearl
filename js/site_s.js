$(document).ready(function() {
 
$("#featured").zAccordion({ 
		easing: 'linear',
		timeout: 3000,
		slideWidth: 600,
		width: 900,
		auto: true, /* Whether or not the slideshow should play automatically. */
trigger: "mouseenter",
		height: 310,
		slideClass: "fry"
	});
 
});